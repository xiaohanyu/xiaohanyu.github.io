.ui.stackable.container
  .ui.very.padded.segment.post
    article.ui.stackable.divided.grid
      header.row.post-title
        .column
          h1.font-kai
            == @item[:title]
      .row.post-body
        section.twelve.wide.column
          == yield
        aside.four.wide.column
          .ui.sub.header Published
          date
            == @item[:created_at].to_date
